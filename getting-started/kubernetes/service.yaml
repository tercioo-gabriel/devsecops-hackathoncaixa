apiVersion: v1
kind: Service
metadata:
  name: quarkus-app-service
spec:
  type: NodePort
  selector:
    app: quarkus-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001